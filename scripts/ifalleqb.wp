// Sets leq to 1 if a <= b, else sets leq to 0.
// Note that this program is overly complicated because it is meant to illustrate an if a <= b else structure.
// The setting of leq itself does not require so much code, but it could be replaced by any other program. 

// Input variables, feel free to change.
a := 3
b := 5

// Program
one := 1
if := b + one
if := if - a
else := 1
while if do
    leq := 1
    else := 0
    if := 0
od
while else do
    leq := 0
    else := 0
od
